#include "main.h"

/**
 * desc int _islower(int c) - checks for lowercase char
 *
 * return 1 if c lowercase or else 0
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
